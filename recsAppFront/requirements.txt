# CPU-Optimized Requirements for Frontend
# This file excludes GPU-specific packages for faster, smaller installation

# Core Streamlit
streamlit==1.56.0

# ML/Data Processing
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
implicit==0.7.2

# Note: LightFM model is expected to be pre-built in app/resources/
# The lightfm package is included in git repository for reference only
# To use pre-built model, download lightfm-<version>-cp310-cp310-none-any.whl
# and place it in app/resources/ directory
rectools-lightfm

# Sentence Transformers (CPU-optimized)
sentence-transformers==5.4.1

# Data handling
openpyxl==3.1.5

# API Client
requests==2.33.1

# Note: PyTorch is installed separately in Dockerfile for CPU-only version
# Install via: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu